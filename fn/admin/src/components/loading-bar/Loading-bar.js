import React from 'react';
import LinearProgress from '@material-ui/core/LinearProgress';

const LoadingBar = () => (
    <LinearProgress style={{ marginTop: '50px', paddingTop: '5px' }} />
);

export default LoadingBar;
