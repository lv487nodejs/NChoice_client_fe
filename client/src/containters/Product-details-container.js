import React from 'react';

import ProductDetails from '../components/product-details';

const ProductDetailsContainer = ({ id }) => <ProductDetails id={id} />;

export default ProductDetailsContainer;
